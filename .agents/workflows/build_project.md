# Build Project Workflow

This workflow outlines the steps to install dependencies and compile the Clash Arena project.

## Steps

1. **Install Dependencies**
   Install the Node.js packages using:
   ```bash
   npm install
   ```

2. **Build the Project**
   Compile the Next.js application:
   ```bash
   npm run build
   ```
